baseURL: "https://snows.github.io/blog/"
languageCode: "ja"
title: "Blog"
theme: "PaperMod"

params:
  defaultTheme: light  # 白背景固定
  ShowShareButtons: true
  ShowReadingTime: true

pagination:
  pagerSize: 300

markup:
  goldmark:
    renderer:
      unsafe: true  # ← これでHTMLタグ（<br>など）が使えるようになります